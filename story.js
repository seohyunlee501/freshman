class Story {
  constructor(_mode) {
    this.mode = _mode;
    console.log("mode:", mode);
  }
  display() {
    if (this.mode == 2) {
      // 지각주 story
    } else if (this.mode == 5) {
      // 고학번 story
    }
  }
}
